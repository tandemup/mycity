import { FadingHeaderDirective } from './fading-header.directive';

describe('FadingHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new FadingHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
